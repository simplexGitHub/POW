{
  "title": "StatusNode",
  "type": "object",
  "description": "The status of each plan node",
  "properties": {
    "PlanIndex": {
      "type": "integer",
      "description": "index of the Node device - used to sync results with original plan - do not change!"
    },
    "sensorId": {
      "type": "integer",
      "description": "Sensor External ID reported from the POD"
    },
    "Status": {
      "type": "string",
      "enum": [
        "Fail",
        "Pass",
        "Aborted",
        "NotDone"
      ],
      "description": "The status of the sensor delivery"
    },
    "ActuallPlacement": {
      "$ref": "./PlacementInfo.json"
    },
    "updateTime": {
      "type": "number",
      "description": "Timestamp format - Epoch   1970"
    }
  },
  "required": [
    "PlanIndex",
    "sensorId",
    "ActuallPlacement"
  ]
}