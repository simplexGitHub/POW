{
  "title": "SensorTypeInfo",
  "type": "object",
  "properties": {
    "SensorTypeID": {
      "$ref": "./SensorTypeID_enum.json"
    },
    "SensorTypeName": {
      "type": "string",
      "description": "Description of sensor - for user readability only"
    },
    "PodTypeID": {
      "type": "string",
      "description": "ID of pod   0=Toss, 1=LowerByWire, 2=Inserted",
      "enum": [
        "Toss",
        "LowerByWire",
        "Inserted"
      ]
    },
    "PodTypeName": {
      "type": "string",
      "description": "Description of pod - for user readability only"
    }
  },
  "required": [
    "SensorTypeID",
    "PodTypeID"
  ]
}